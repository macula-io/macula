[
    {macula, [
        %% Bootstrap-only mode
        {mode, bootstrap},
        {realm, <<"macula.bootstrap">>},
        {gateway_port, 4433},

        %% TLS certificates - use same paths as macula_tls:get_cert_paths/0 defaults
        %% Auto-generated if missing at startup
        {cert_path, "/var/lib/macula/cert.pem"},
        {key_path, "/var/lib/macula/key.pem"},

        %% Health check interval (60 seconds)
        {bootstrap_health_interval, 60000},

        %% Logging
        {log_level, info}
    ]},

    {kernel, [
        {logger_level, info},
        {logger, [
            {handler, default, logger_std_h, #{
                formatter => {logger_formatter, #{
                    single_line => true,
                    template => [time," [",level,"] ",msg,"\n"]
                }}
            }}
        ]}
    ]},

    {sasl, [
        {sasl_error_logger, false}
    ]}
].
